# ========== 测试环境核心：H2 内存数据库，替代外部MySQL ==========
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:ganlu_test;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_LOWER=TRUE
spring.datasource.username=sa
spring.datasource.password=

# =====# ========== 测试库自动初始化：标准配置，仅执行一次 ==========
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema-test.sql
spring.sql.init.data-locations=classpath:data-test.sql
spring.sql.init.encoding=UTF-8
spring.sql.init.continue-on-error=false

# ========== 以下完全复用主配置通用项，与正式环境逻辑一致 ==========
spring.application.name=ganlu
mybatis.mapper-locations=mapper/*.xml

spring.thymeleaf.enabled=false
spring.thymeleaf.cache=false
spring.freemarker.enabled=true
spring.freemarker.cache=false
spring.freemarker.suffix=.ftl
spring.freemarker.template-loader-path=/WEB-INF/jsp/
spring.freemarker.request-context-attribute=request

spring.web.resources.static-locations=/WEB-INF/

spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-file-size=200MB

security.jwt.expiration-seconds=28800

# ========== 补齐dev环境的所有专属配置（结构与dev完全一致，仅替换为测试路径） ==========
# 文件上传根目录（测试环境用target下的临时目录，clean自动清理）
file.upload-dir=./target/test-uploads
# 保持与dev一致的嵌套引用结构，保证Bean初始化逻辑完全相同
file.chunk-dir=${file.upload-dir}/temp_chunks
file.materials-dir=${file.upload-dir}/materials
file.images-dir=${file.upload-dir}/images

# JWT签名密钥（测试环境固定值，不依赖环境变量）
security.jwt.secret=ganlu-test-jwt-secret-2026-ustb-practice-team-website
# 甘露网站 GitHub 协作规范

## 1. 协作目标

本项目采用“一个公有仓库、`main` 主分支、短期功能分支、Pull Request 审查合并”的协作方式。

孙木文负责创建和管理 GitHub 公有仓库，并邀请其余 8 位成员成为协作者。所有成员从最新的 `main` 分支创建任务分支，在任务分支中开发、测试并提交 Pull Request；审查通过后再合并至 `main`。

不得采用“一人一个长期分支，最后一次性合并”的方式。每个分支只对应一个范围明确、可以独立测试的任务，完成合并后及时删除。

## 2. 人员与职责

| 成员 | 主要职责 | GitHub 协作职责 |
|---|---|---|
| 孙木文 | 团队风采公开端前端 | 仓库所有者，邀请成员、配置仓库规则、管理版本发布 |
| 乔轩轲 | AI 小助手前端 | 与陈家顺确定接口并互相审查 |
| 陈家顺 | AI 小助手后端 | 保护服务端密钥，提供接口、测试并与前端联调 |
| 关振卿 | 互动留言板前端 | 与方亦琳确定接口并互相审查 |
| 王嘉阳 | 团队风采核心后端 | 负责年份、小队、详情元数据及管理员建团接口 |
| 肖约宝 | 课件共享与网站联调 | 负责课件功能，并配合赵友为完成整体联调 |
| 赵友为 | 网站公共架构与总集成 | 技术总集成，统一处理共享文件、路由、配置和合并冲突 |
| 方亦琳 | 互动留言板后端 | 负责数据库、权限、分页、删除逻辑与后端测试 |
| 李嘉辉 | 团队风采内容管理 | 负责成员、照片、日志、荣誉、附件上传和审核功能 |

孙木文负责仓库管理，赵友为负责技术集成。原则上由二人执行最终合并；其他成员完成代码提交、Pull Request、审查和测试。

> 个人账号仓库只有“所有者”和“协作者”两级权限。协作者具备写入以及创建、关闭和合并 Pull Request 的能力，因此“仅由孙木文、赵友为合并”同时依赖 `main` 分支规则和团队约定。如果后期需要更细粒度的权限，可以把仓库迁移到 GitHub Organization。

## 3. 核心协作规则

1. `main` 必须始终保持可以构建、可以测试，禁止直接在 `main` 上开发。
2. 一个任务建立一个短期分支，不建立长期个人分支。
3. 所有代码修改都通过 Pull Request 合并，禁止直接推送到 `main`。
4. 每个 Pull Request 只处理一个主题，避免同时混入无关修改。
5. 涉及公共文件的修改必须经过赵友为审查。
6. 提交前必须完成对应的前端构建或后端测试。
7. 禁止提交密码、Token、API Key、运行时上传文件和构建产物。
8. 数据库负责人提交独立增量 SQL，不得相互覆盖根目录 `ganlu.sql`。

## 4. 首次创建仓库前的安全整理

### 4.1 必须排除的内容

项目根目录应建立 `.gitignore`，至少包含：

```gitignore
# Frontend dependencies and build output
frontend/node_modules/
frontend/dist/

# Backend build output and runtime uploads
backend/target/
backend/uploads/

# Local configuration and secrets
.env
.env.*
!.env.example
backend/src/main/resources/application-local.properties

# IDE files
.idea/
.vscode/
*.iml

# Logs and operating-system files
*.log
Thumbs.db
.DS_Store
```

### 4.2 配置与密钥

数据库密码、JWT 密钥和 DeepSeek API Key 必须通过后端环境变量提供。例如：

```properties
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
security.jwt.secret=${JWT_SECRET}
```

DeepSeek 密钥使用环境变量 `DEEPSEEK_API_KEY`，不得写入 Vue 文件、Properties、SQL、`.env.*` 或 GitHub 仓库。

可以提交 `.env.example` 或 `application-local.properties.example`，但示例文件只能包含变量名和说明，不能包含真实值。

### 4.3 不得公开的业务文件

`backend/uploads/` 中的成员照片、课件、视频、临时分片和用户上传内容均不得进入源代码仓库。公开照片、教学材料或其他素材前，必须确认已经获得肖像、隐私和版权方面的授权。

## 5. 孙木文的首次建仓流程

### 5.1 初始化本地仓库

确认项目已经完成安全整理后，在项目根目录执行：

```powershell
git init -b main
git add .
git status --short
git diff --cached --stat
```

提交前确认暂存区中没有：

- `frontend/node_modules/`
- `frontend/dist/`
- `backend/target/`
- `backend/uploads/`
- 真实数据库密码、JWT 密钥或 API Key
- 未经授权的照片、课件和视频

确认无误后建立基线提交：

```powershell
git commit -m "chore: establish project baseline"
```

如果项目根目录已有一个无效或空的 `.git` 目录，应先将其移出项目备份，再执行 `git init -b main`。

### 5.2 创建 GitHub 公有仓库

1. 在 GitHub 点击 `New repository`。
2. 建议仓库名使用 `ganlu-webpage`。
3. 可见性选择 `Public`。
4. 不要在网页端自动创建 README、`.gitignore` 或 LICENSE，避免与本地基线冲突。
5. 创建后复制仓库 HTTPS 地址。

在本地关联并推送：

```powershell
git remote add origin https://github.com/<孙木文的GitHub用户名>/ganlu-webpage.git
git remote -v
git push -u origin main
```

### 5.3 邀请协作者

进入：

```text
仓库 → Settings → Collaborators → Add people
```

邀请其余 8 位成员，并确认所有成员都已接受邀请。

### 5.4 保护 `main`

进入：

```text
仓库 → Settings → Rules → Rulesets → New branch ruleset
```

规则目标选择默认分支 `main`，建议启用：

- Require a pull request before merging
- Required approvals：至少 1 人
- Require conversation resolution before merging
- Block force pushes
- Restrict deletions
- CI 建立并稳定运行后启用 Require status checks to pass

仓库合并方式建议只保留 `Squash merge`，并启用合并后自动删除分支。

## 6. 分支管理

### 6.1 分支命名

使用小写英文、数字和连字符：

```text
feature/<Issue编号>-<功能名称>
fix/<Issue编号>-<问题名称>
docs/<Issue编号>-<文档名称>
refactor/<Issue编号>-<重构名称>
```

示例：

```text
feature/12-fengcai-year-list
feature/18-ai-chat-frontend
feature/19-ai-chat-backend
feature/23-message-pagination
feature/28-course-upload
fix/35-login-token
docs/41-api-contract
```

### 6.2 创建任务分支

每次开始新任务前必须同步最新 `main`：

```powershell
git switch main
git pull --ff-only origin main
git switch -c feature/18-ai-chat-frontend
```

不要从其他尚未合并的功能分支创建新分支，除非该任务确实依赖对方且已经与赵友为确认。

## 7. 日常开发与提交

### 7.1 首次克隆

```powershell
git clone https://github.com/<孙木文的GitHub用户名>/ganlu-webpage.git
cd ganlu-webpage
```

### 7.2 提交修改

开发完成后先查看改动：

```powershell
git status
git diff
```

只添加与当前任务有关的文件：

```powershell
git add frontend/src/views/MessageBoard.vue
git commit -m "feat(message): add paginated message list"
git push -u origin feature/23-message-pagination
```

日常开发不建议直接执行 `git add .`，避免把无关文件或本地配置加入提交。

### 7.3 提交信息格式

```text
<类型>(<模块>): <简短说明>
```

常用类型：

| 类型 | 用途 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修复问题 |
| `docs` | 文档修改 |
| `refactor` | 不改变功能的代码重构 |
| `test` | 测试 |
| `chore` | 配置、构建或维护工作 |

示例：

```text
feat(ai): implement chat loading state
fix(message): enforce reply permission
docs(course): document upload API
test(fengcai): add team detail service tests
```

## 8. Pull Request 流程

推送任务分支后，在 GitHub 创建从任务分支到 `main` 的 Pull Request。

Pull Request 必须包含：

1. 关联 Issue，例如 `Closes #23`。
2. 本次修改解决的问题。
3. 主要修改内容和涉及文件。
4. 是否修改接口、数据库或配置。
5. 测试步骤和测试结果。
6. 前端页面截图或后端接口响应示例。
7. 是否涉及公共文件及需要的审查人。

建议模板：

```markdown
## 关联任务

Closes #编号

## 修改内容

- 

## 接口、数据库或配置变化

- 无 / 请说明

## 测试结果

- [ ] 前端构建通过
- [ ] 后端测试通过
- [ ] 已完成手工测试

## 截图或接口结果

请附上必要证据。

## 审查注意事项

- 
```

## 9. 审查与合并规则

### 9.1 模块审查

- AI 小助手：乔轩轲与陈家顺互相审查。
- 互动留言板：关振卿与方亦琳互相审查。
- 团队风采：孙木文、王嘉阳、李嘉辉交叉审查。
- 课件共享：肖约宝完成自测，由赵友为检查整体联调影响。
- 公共架构、路由、配置和共享文件：必须由赵友为审查。

### 9.2 合并条件

只有同时满足以下条件才能合并：

- Pull Request 范围清晰，没有无关修改。
- 至少 1 位指定审查人批准。
- 所有审查意见已经解决。
- 分支与最新 `main` 没有未解决冲突。
- 前端构建或后端测试通过。
- 没有提交密钥、密码、上传文件或构建产物。

合并时使用 `Squash and merge`。合并后删除远程任务分支，所有成员在开始下一项任务前重新同步 `main`。

## 10. 共享文件所有权

以下文件由赵友为统一修改和集成。其他成员需要改动时，应在 Pull Request 说明中列出所需变化，避免直接覆盖：

```text
frontend/src/router/index.js
frontend/src/components/Top.vue
frontend/src/components/Foot.vue
frontend/src/utils/http.js
frontend/src/utils/path.js
frontend/src/utils/access.js
backend/pom.xml
backend/src/main/resources/application.properties
backend/src/main/resources/application-dev.properties
backend/src/main/resources/application-prod.properties
ganlu.sql
```

如果任务无法避免修改共享文件，必须提前通知赵友为，并在 Pull Request 中将赵友为设为审查人。

## 11. 数据库协作

各后端负责人在 `database/patches/` 中提交独立增量 SQL：

```text
database/patches/20260729-fangyilin-message-table.sql
database/patches/20260729-wangjiayang-team-page.sql
database/patches/20260729-lijiahui-content-review.sql
```

增量脚本应：

- 文件名包含日期、负责人和功能。
- 尽量可以重复检查，避免无条件破坏已有数据。
- 说明执行顺序和依赖。
- 不包含真实用户数据、密码、Token 或其他敏感数据。
- 由赵友为在总联调后统一合并到基线 SQL。

## 12. 同步主分支与处理冲突

创建 Pull Request 前同步最新主分支：

```powershell
git fetch origin
git merge origin/main
```

发生冲突时：

1. 先确认冲突文件属于哪个成员或共享文件负责人。
2. 普通功能文件由对应负责人解决。
3. 路由、公共组件、配置、`pom.xml` 和总 SQL 等共享文件由赵友为决定最终内容。
4. 禁止直接用自己的完整旧文件覆盖他人修改。
5. 解决后重新执行相关构建和测试，再推送任务分支。

未经团队确认，不要对共享分支执行强制推送。任何需要重写远程历史的操作都必须先通知相关成员。

## 13. 提交前测试

### 13.1 前端

```powershell
cd frontend
npm ci
npm run build
```

### 13.2 后端

```powershell
cd backend
.\mvnw.cmd test
```

每位成员还需要保存至少 5 条与自己功能相关的手工测试记录，包括操作步骤、预期结果、实际结果和测试账号角色。

## 14. 推荐集成顺序

1. 孙木文建立经过脱敏的公有仓库基线，并邀请所有成员。
2. 赵友为完成环境配置模板、Token 认证骨架和前端请求拦截器。
3. 王嘉阳、方亦琳、陈家顺、肖约宝、李嘉辉分别提交后端接口和独立 SQL 补丁。
4. 孙木文、乔轩轲、关振卿、肖约宝完成对应前端页面和模块联调。
5. 赵友为统一合并路由、菜单、公共配置和基线 SQL，并解决跨模块问题。
6. 全员在 Windows 11 上使用游客、学生、团队账号和管理员账号完成验收。
7. 验收通过后，由孙木文创建版本标签和 GitHub Release。

## 15. 禁止事项

- 禁止直接向 `main` 推送功能代码。
- 禁止建立长期不合并的个人分支。
- 禁止在一个 Pull Request 中混合多个无关任务。
- 禁止未经沟通修改或覆盖共享文件。
- 禁止提交 `node_modules`、`dist`、`target`、`uploads` 和 IDE 配置。
- 禁止提交数据库密码、JWT 密钥、API Key、Token 或真实用户数据。
- 禁止把大体积课件、视频和运行时上传文件作为普通 Git 文件提交。
- 禁止未经沟通使用强制推送或重写共享历史。
- 禁止只在最后一天集中合并所有成员的长期分支。

## 16. 协作流程总览

```text
创建并分配 Issue
        ↓
从最新 main 创建短期任务分支
        ↓
在任务分支中开发和本地测试
        ↓
提交并推送任务分支
        ↓
创建 Pull Request
        ↓
模块搭档或负责人审查
        ↓
涉及共享文件时由赵友为审查
        ↓
解决意见、冲突并通过构建/测试
        ↓
孙木文或赵友为 Squash 合并到 main
        ↓
删除任务分支并同步最新 main
```

## 17. GitHub 官方参考

- [个人账号仓库权限](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/repository-access-and-collaboration/permission-levels-for-a-personal-account-repository)
- [邀请个人仓库协作者](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/repository-access-and-collaboration/inviting-collaborators-to-a-personal-repository)
- [GitHub Rulesets 可用规则](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/available-rules-for-rulesets)
- [将本地代码添加到 GitHub](https://docs.github.com/en/migrations/importing-source-code/using-the-command-line-to-import-source-code/adding-locally-hosted-code-to-github)
- [GitHub 大文件限制](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)
